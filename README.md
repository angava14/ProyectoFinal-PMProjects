Sistema de Informacion para crear documentos dinamicos relacionada a proyectos tipo PMP.

Instalacion:

npm install  => para instalar 
npm run build = > para compilar
npm start => para iniciar el servidor